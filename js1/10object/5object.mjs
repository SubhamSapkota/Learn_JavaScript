let info ={
  name: "nitan"
};

console.log(info.name)
console.log(info.age.name) //undefined

//it will throw error
//we cannot use . for undefined
//cannot read properties of an undefined(reading 'name')