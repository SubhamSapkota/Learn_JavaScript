let info = {
  name: "nitan",
  surname: "thapa",
  age: 30,
  address: "gagalphedi"
};

let myInfo = `My name is ${info.name} ${info.surname}. My age is ${info.age}. I live in ${info.address}.`
console.log(myInfo);